Sub CopySheetsToTemplate()

    ' Copies worksheets from multiple Excel workbooks
    ' into a standardized template.
    
    ' File paths should be selected dynamically
    ' rather than stored as personal local paths.

End Sub
